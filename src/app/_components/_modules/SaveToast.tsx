'use client';
import styles from './SaveToast.module.scss';

const SaveToast = () => {
  return (
    <div className={styles.toastContent}>
      <p>임시저장을 완료했어요!</p>
    </div>
  );
};

export default SaveToast;
