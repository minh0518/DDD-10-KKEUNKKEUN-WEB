'use client';
import styles from './AlarmToast.module.scss';

const AlarmToast = () => {
  return (
    <div className={styles.toastContent}>
      <p>알람</p>
    </div>
  );
};

export default AlarmToast;
